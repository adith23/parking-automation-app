# Ignore unnecessary files and folders to drastically reduce archive size.

# Dependencies - These are re-installed on the remote build machine.
# Your project directory shows a very large 'node_modules' folder (typical cause of large size).
node_modules/
**node_modules/**

# Cache directories
.expo/
.yarn/cache/
.npm/
.gradle/
.bin/

# Temporary and output directories
build/
dist/
temp/
coverage/
web-build/
.next/

# Git and editor files
.git/
.gitignore
.gitattributes
.svn/
.hg/
.vscode/

# Logs and lock files (except package-lock.json/yarn.lock/etc.)
*.log
npm-debug.log*
yarn-debug.log*

# IDE/OS generated files
*.iml
*.iws
*.ipr
*.idea/
.DS_Store